---
layout: default
title: About The Group
---

<div class="flex flex-col items-center text-lg">
    {% if author.avatar %}
    <a href="{{ author.homepage | url }}" target="_blank" rel="noopener">
        <img class="w-32 h-32 md:w-40 md:h-40 rounded-full" src="{{ author.avatar | url }}" width="128" height="128" alt="Author avatar">
    </a>
    {% endif %}
    <div class="text-center mt-8">
        {% if author.name1 %}
        <div class="text-slate-600">
            <div href="mailto:{{ author.name1 }}">{{ author.name1 }}</div>
        </div>
        {% endif %}
        {% if author.name2 %}
        <div class="text-slate-600">
            <div href="mailto:{{ author.name2 }}">{{ author.name2 }}</div>
        </div>
        {% endif %}
        {% if author.name3 %}
        <div class="text-slate-600">
            <div href="mailto:{{ author.name3 }}">{{ author.name3 }}</div>
        </div>
        {% endif %}
        {% if author.name4 %}
        <div class="text-slate-600">
            <div href="mailto:{{ author.name4 }}">{{ author.name4 }}</div>
        </div>
        {% endif %}

    </div>
</div>